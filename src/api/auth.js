import request from './request'

const URL = {
  REGISTER: '/user/register',
  LOGIN: '/user/login',
  LOGOUT: '/user/logout',
  GET_INFO: '/user/info'
}

export default {
  register ({ username, password, email }) {

  }
}
